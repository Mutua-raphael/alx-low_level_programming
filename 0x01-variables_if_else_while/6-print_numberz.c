#include<stdio.h>

/**
 * main - Prints numbers from 0 to 9
 *
 * Returns: 0 if all has succeded
 */
int main(void)
{
	int i;

	for (i = 87; i < 97; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
